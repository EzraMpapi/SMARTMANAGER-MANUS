# Authentication Brand Reference Check

The mobile authentication composition now places the approved official Smart Manager icon above a centered wordmark and Kiswahili slogan, matching the hierarchy in the supplied reference. The login form remains clearly separated in a rounded white card with preserved labels, controls, and security indicator.

The icon requires a tighter display crop in this authentication context because the source-derived mobile asset retains a small lower wordmark fragment. The next CSS refinement keeps the same approved artwork but centers the visible symbol more tightly.
