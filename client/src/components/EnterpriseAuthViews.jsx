import React, { useEffect, useState } from "react";
import { AlertCircle, CheckCircle2, Eye, EyeOff, Lock, Mail, ShieldCheck } from "lucide-react";
import { PASSWORD_REQUIREMENT_LABELS, getPasswordChecks, isEnterprisePassword, passwordStrength } from "../lib/authOnboarding";
import { BrandLogo } from "./BrandLogo";
import { AuthModuleShowcase } from "./AuthModuleShowcase";
import { useLanguage } from "../contexts/LanguageContext";

function LegacyEnterpriseAuthShell({ title, subtitle, children, asideTitle = "Operate with clarity.", asideCopy = "One secure workspace for the people, processes, and decisions that move your organisation forward." }) {
  const { lang, setLang } = useLanguage();
  return <div className="min-h-screen bg-[#F4F7F6] text-slate-950" style={{ fontFamily: "'Inter', system-ui, sans-serif" }}><div className="mx-auto flex min-h-screen max-w-[1600px]"><aside className="relative hidden w-[44%] overflow-hidden bg-[#0B2D22] px-12 py-10 lg:flex lg:flex-col lg:justify-between"><div className="absolute inset-0 opacity-30" style={{ backgroundImage: "linear-gradient(rgba(255,255,255,.06) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.06) 1px, transparent 1px)", backgroundSize: "54px 54px" }} /><div className="relative z-10 flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-500 text-lg font-bold text-white">S</div><div><p className="text-[16px] font-bold tracking-tight text-white">Smart Manager</p><p className="mt-0.5 text-[10px] font-semibold uppercase tracking-[.18em] text-emerald-200/70">Enterprise ERP</p></div></div><div className="relative z-10 max-w-md pb-16"><p className="mb-5 text-[11px] font-bold uppercase tracking-[.18em] text-emerald-300">Secure business workspace</p><h1 className="max-w-[11ch] text-[44px] font-bold leading-[1.04] tracking-[-.045em] text-white" style={{ fontFamily: "'Poppins', sans-serif" }}>{asideTitle}</h1><p className="mt-6 max-w-md text-[15px] leading-7 text-emerald-50/70">{asideCopy}</p><div className="mt-10 grid grid-cols-3 gap-3 border-t border-white/10 pt-6">{[["Security", "Tenant-aware"], ["Control", "Role-based"], ["Ready", "Modular ERP"]].map(([label, value]) => <div key={label}><p className="text-[10px] font-semibold uppercase tracking-wider text-emerald-200/50">{label}</p><p className="mt-1 text-[12px] font-semibold text-white">{value}</p></div>)}</div></div><p className="relative z-10 text-[11px] text-emerald-100/45">© {new Date().getFullYear()} Smart Manager · Enterprise Business Ecosystem</p></aside><main className="flex flex-1 items-center justify-center px-5 py-8 sm:px-10 lg:px-16"><section className="w-full max-w-[430px]"><div className="mb-8 flex items-center justify-between lg:hidden"><div className="flex items-center gap-2.5"><div className="grid h-9 w-9 place-items-center rounded-xl bg-emerald-600 text-sm font-bold text-white">S</div><div><p className="text-[14px] font-bold">Smart Manager</p><p className="text-[9px] font-semibold uppercase tracking-[.16em] text-slate-400">Enterprise ERP</p></div></div><ShieldCheck size={19} className="text-emerald-700" aria-label="Secure workspace" /></div><div className="rounded-[22px] border border-slate-200/90 bg-white p-6 shadow-[0_18px_50px_rgba(15,23,42,.08)] sm:p-8"><div className="mb-7"><p className="mb-2 text-[10px] font-bold uppercase tracking-[.17em] text-emerald-700">Smart Manager workspace</p><h2 className="text-[25px] font-bold tracking-[-.035em] text-slate-950" style={{ fontFamily: "'Poppins', sans-serif" }}>{title}</h2><p className="mt-2 text-[13.5px] leading-6 text-slate-500">{subtitle}</p></div>{children}</div><div className="mt-5 flex items-center justify-center gap-3 text-[11px] leading-5 text-slate-400"><p>Protected by secure authentication and tenant-aware access controls.</p><button type="button" onClick={() => setLang(lang === "en" ? "sw" : "en")} className="rounded-full border border-slate-200 bg-white px-2.5 py-1 font-semibold text-slate-500 transition hover:border-emerald-300 hover:text-emerald-700" aria-label={lang === "en" ? "Switch authentication language to Kiswahili" : "Switch authentication language to English"}>{lang === "en" ? "Kiswahili" : "English"}</button></div></section></main></div></div>;
}

export function EnterpriseAuthShell({ title, subtitle, children, asideTitle = "Operate with clarity.", asideCopy = "One secure workspace for the people, processes, and decisions that move your organisation forward." }) {
  const { lang, setLang } = useLanguage();
  return <div className="min-h-screen bg-[#F4F7F6] text-slate-950" style={{ fontFamily: "'Inter', system-ui, sans-serif" }}><div className="mx-auto flex min-h-screen max-w-[1600px]"><aside className="relative hidden w-[44%] overflow-hidden bg-[#0B2D22] px-12 py-10 lg:flex lg:flex-col lg:justify-between"><div className="absolute inset-0 opacity-30" style={{ backgroundImage: "linear-gradient(rgba(255,255,255,.06) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,.06) 1px, transparent 1px)", backgroundSize: "54px 54px" }} /><div className="relative z-10 flex items-center gap-3"><BrandLogo variant="full" priority className="w-[clamp(160px,18vw,260px)] h-auto drop-shadow-[0_12px_28px_rgba(0,0,0,.18)]"/></div><AuthModuleShowcase /><div className="relative z-10 max-w-md pb-16"><p className="mb-5 text-[11px] font-bold uppercase tracking-[.18em] text-emerald-300">Secure business workspace</p><h1 className="max-w-[11ch] text-[44px] font-bold leading-[1.04] tracking-[-.045em] text-white" style={{ fontFamily: "'Poppins', sans-serif" }}>{asideTitle}</h1><p className="mt-6 max-w-md text-[15px] leading-7 text-emerald-50/70">{asideCopy}</p><div className="mt-10 grid grid-cols-3 gap-3 border-t border-white/10 pt-6">{[["Security", "Tenant-aware"], ["Control", "Role-based"], ["Ready", "Modular ERP"]].map(([label, value]) => <div key={label}><p className="text-[10px] font-semibold uppercase tracking-wider text-emerald-200/50">{label}</p><p className="mt-1 text-[12px] font-semibold text-white">{value}</p></div>)}</div></div><p className="relative z-10 text-[11px] text-emerald-100/45">© {new Date().getFullYear()} Smart Manager · Enterprise Business Ecosystem</p></aside><main className="flex flex-1 items-center justify-center px-5 py-8 sm:px-10 lg:px-16"><section className="w-full max-w-[430px]"><div className="mb-8 flex items-center justify-between lg:hidden"><BrandLogo variant="full" priority className="w-[clamp(150px,52vw,215px)] h-auto"/><ShieldCheck size={19} className="text-emerald-700" aria-label="Secure workspace" /></div><AuthModuleShowcase compact /><div className="rounded-[22px] border border-slate-200/90 bg-white p-6 shadow-[0_18px_50px_rgba(15,23,42,.08)] sm:p-8"><div className="mb-7"><p className="mb-2 text-[10px] font-bold uppercase tracking-[.17em] text-emerald-700">Smart Manager workspace</p><h2 className="text-[25px] font-bold tracking-[-.035em] text-slate-950" style={{ fontFamily: "'Poppins', sans-serif" }}>{title}</h2><p className="mt-2 text-[13.5px] leading-6 text-slate-500">{subtitle}</p></div>{children}</div><div className="mt-5 flex items-center justify-center gap-3 text-[11px] leading-5 text-slate-400"><p>Protected by secure authentication and tenant-aware access controls.</p><button type="button" onClick={() => setLang(lang === "en" ? "sw" : "en")} className="rounded-full border border-slate-200 bg-white px-2.5 py-1 font-semibold text-slate-500 transition hover:border-emerald-300 hover:text-emerald-700" aria-label={lang === "en" ? "Switch authentication language to Kiswahili" : "Switch authentication language to English"}>{lang === "en" ? "Kiswahili" : "English"}</button></div></section></main></div></div>;
}

export function AuthNotice({ type = "error", children }) {
  const success = type === "success";
  return <div role={success ? "status" : "alert"} className={`mb-5 flex items-start gap-2.5 rounded-xl border px-3.5 py-3 text-[12.5px] leading-5 ${success ? "border-emerald-100 bg-emerald-50 text-emerald-800" : "border-red-100 bg-red-50 text-red-800"}`}><span className="mt-0.5 shrink-0">{success ? <CheckCircle2 size={15}/> : <AlertCircle size={15}/>}</span><span>{children}</span></div>;
}

export function PasswordStrengthMeter({ password }) {
  const checks = getPasswordChecks(password); const strength = passwordStrength(password); const labels = ["Very weak", "Weak", "Fair", "Good", "Strong"];
  if (!password) return null;
  return <div className="rounded-xl border border-slate-100 bg-slate-50/70 p-3"><div className="flex items-center justify-between"><p className="text-[11px] font-semibold text-slate-600">Password strength</p><p className={`text-[11px] font-semibold ${strength >= 4 ? "text-emerald-700" : "text-amber-700"}`}>{labels[Math.max(0, strength - 1)]}</p></div><div className="mt-2 grid grid-cols-5 gap-1">{Array.from({ length: 5 }, (_, i) => <span key={i} className={`h-1 rounded-full ${i < strength ? (strength >= 4 ? "bg-emerald-500" : "bg-amber-400") : "bg-slate-200"}`} />)}</div><div className="mt-3 grid gap-1.5 sm:grid-cols-2">{PASSWORD_REQUIREMENT_LABELS.map(([key, label]) => <p key={key} className={`flex items-center gap-1.5 text-[10.5px] ${checks[key] ? "text-emerald-700" : "text-slate-400"}`}><CheckCircle2 size={12} />{label}</p>)}</div></div>;
}

export function EnterpriseLoginView({ onSignIn, onSignup, onForgot, onOAuth, toMessage, configured, initialError = null }) {
  const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState(null);
  useEffect(() => { if (initialError) setError(initialError); }, [initialError]);
  async function submit(e) { e.preventDefault(); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim()) || !password) { setError("Enter your work email and password to continue."); return; } setBusy(true); setError(null); try { await onSignIn(email.trim(), password); } catch (signInError) { setError(toMessage(signInError)); } finally { setBusy(false); } }
  return <EnterpriseAuthShell title="Welcome back" subtitle="Sign in to your organisation’s secure Smart Manager workspace." asideTitle="Your operations, in command." asideCopy="Bring finance, sales, people, inventory, and insights into a workspace designed for accountable execution."><form onSubmit={submit} className="space-y-4">{error && <AuthNotice>{error}</AuthNotice>}<label className="block text-[12px] font-semibold text-slate-700">Work email<input type="email" autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" className="mt-1.5 w-full rounded-xl border border-slate-200 px-4 py-3 text-[13.5px] outline-none transition focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100" /></label><label className="block text-[12px] font-semibold text-slate-700">Password<div className="relative mt-1.5"><input type={showPassword ? "text" : "password"} autoComplete="current-password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password" className="w-full rounded-xl border border-slate-200 px-4 py-3 pr-11 text-[13.5px] outline-none transition focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100" /><button type="button" onClick={() => setShowPassword((value) => !value)} aria-label={showPassword ? "Hide password" : "Show password"} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-700">{showPassword ? <EyeOff size={16}/> : <Eye size={16}/>}</button></div></label><div className="flex justify-end"><button type="button" onClick={onForgot} className="text-[12px] font-semibold text-emerald-700 hover:text-emerald-900 hover:underline">Forgot password?</button></div><button type="submit" disabled={busy} className="w-full rounded-xl bg-[#0B5D3B] py-3.5 text-[13.5px] font-semibold text-white shadow-sm transition hover:bg-[#084B30] disabled:cursor-not-allowed disabled:opacity-50">{busy ? "Signing you in…" : "Sign in securely"}</button>{configured && <><div className="flex items-center gap-3 py-1 text-[10px] font-semibold uppercase tracking-[.14em] text-slate-400"><span className="h-px flex-1 bg-slate-200"/>or continue with<span className="h-px flex-1 bg-slate-200"/></div><div className="grid grid-cols-3 gap-2"><button type="button" disabled={busy} onClick={() => onOAuth("google")} className="rounded-xl border border-slate-200 py-2.5 text-[11px] font-semibold text-slate-700 transition hover:bg-slate-50">Google</button><button type="button" disabled={busy} onClick={() => onOAuth("azure")} className="rounded-xl border border-slate-200 py-2.5 text-[11px] font-semibold text-slate-700 transition hover:bg-slate-50">Microsoft</button><button type="button" disabled={busy} onClick={() => onOAuth("apple")} className="rounded-xl border border-slate-200 py-2.5 text-[11px] font-semibold text-slate-700 transition hover:bg-slate-50">Apple</button></div><p className="text-center text-[10.5px] leading-5 text-slate-400">Use the same provider you used when your workspace account was created.</p></>}<p className="pt-1 text-center text-[12.5px] text-slate-500">New to Smart Manager? <button type="button" onClick={onSignup} className="font-semibold text-emerald-700 hover:underline">Create your workspace</button></p></form></EnterpriseAuthShell>;
}

export function ForgotPasswordView({ onBack, onRequest, toMessage }) {
  const [email, setEmail] = useState(""); const [busy, setBusy] = useState(false); const [error, setError] = useState(null); const [sent, setSent] = useState(false);
  async function submit(e) { e.preventDefault(); if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email.trim())) { setError("Enter a valid work email address."); return; } setBusy(true); setError(null); try { await onRequest(email.trim()); setSent(true); } catch (requestError) { setError(toMessage(requestError)); } finally { setBusy(false); } }
  return <EnterpriseAuthShell title={sent ? "Check your inbox" : "Recover access"} subtitle={sent ? "If the address is associated with a password-based workspace account, a secure reset link is on its way." : "Enter your work email and we’ll send a secure password reset link."} asideTitle="Access is recoverable." asideCopy="Password recovery is private by design. We never disclose whether a particular email address has an account.">{sent ? <div className="space-y-4"><AuthNotice type="success">For security, this confirmation is the same whether or not the address is registered. Check your inbox and spam folder for a Smart Manager recovery link.</AuthNotice><button type="button" onClick={onBack} className="w-full rounded-xl border border-slate-200 py-3 text-[13px] font-semibold text-slate-700 transition hover:bg-slate-50">Back to sign in</button></div> : <form onSubmit={submit} className="space-y-5">{error && <AuthNotice>{error}</AuthNotice>}<label className="block text-[12px] font-semibold text-slate-700">Work email<input type="email" autoComplete="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="you@company.com" className="mt-1.5 w-full rounded-xl border border-slate-200 px-4 py-3 text-[13.5px] outline-none transition focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100" /></label><button type="submit" disabled={busy} className="w-full rounded-xl bg-[#0B5D3B] py-3.5 text-[13.5px] font-semibold text-white shadow-sm transition hover:bg-[#084B30] disabled:opacity-50">{busy ? "Sending secure link…" : "Send reset link"}</button><button type="button" onClick={onBack} className="mx-auto block text-[12.5px] font-semibold text-slate-500 hover:text-slate-800">Back to sign in</button></form>}</EnterpriseAuthShell>;
}

export function ResetPasswordView({ recoveryToken, onBack, onUpdate, toMessage }) {
  const [password, setPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState(""); const [showPassword, setShowPassword] = useState(false); const [busy, setBusy] = useState(false); const [error, setError] = useState(null); const [complete, setComplete] = useState(false);
  async function submit(e) { e.preventDefault(); if (!isEnterprisePassword(password)) { setError("Use a password that meets all the requirements below."); return; } if (password !== confirmPassword) { setError("The password confirmation does not match."); return; } setBusy(true); setError(null); try { await onUpdate(recoveryToken, password); setComplete(true); } catch (updateError) { setError(updateError?.code === "RECOVERY_SESSION_MISSING" ? "This reset link is invalid or has expired. Request a new one to continue." : toMessage(updateError)); } finally { setBusy(false); } }
  return <EnterpriseAuthShell title={complete ? "Password updated" : "Choose a new password"} subtitle={complete ? "Your password has been changed securely. Sign in with your new credentials." : "Create a strong password to protect your business workspace."} asideTitle="Security starts here." asideCopy="A valid recovery session is required before Smart Manager can update a password.">{complete ? <div className="space-y-4"><AuthNotice type="success">Your password has been updated successfully.</AuthNotice><button type="button" onClick={onBack} className="w-full rounded-xl bg-[#0B5D3B] py-3.5 text-[13.5px] font-semibold text-white">Continue to sign in</button></div> : <form onSubmit={submit} className="space-y-4">{error && <AuthNotice>{error}</AuthNotice>}<label className="block text-[12px] font-semibold text-slate-700">New password<div className="relative mt-1.5"><input type={showPassword ? "text" : "password"} autoComplete="new-password" value={password} onChange={(e) => setPassword(e.target.value)} className="w-full rounded-xl border border-slate-200 px-4 py-3 pr-11 text-[13.5px] outline-none transition focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100" /><button type="button" onClick={() => setShowPassword((value) => !value)} aria-label={showPassword ? "Hide password" : "Show password"} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-700">{showPassword ? <EyeOff size={16}/> : <Eye size={16}/>}</button></div></label><label className="block text-[12px] font-semibold text-slate-700">Confirm new password<input type={showPassword ? "text" : "password"} autoComplete="new-password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} className="mt-1.5 w-full rounded-xl border border-slate-200 px-4 py-3 text-[13.5px] outline-none transition focus:border-emerald-600 focus:ring-2 focus:ring-emerald-100" /></label><PasswordStrengthMeter password={password}/><button type="submit" disabled={busy} className="w-full rounded-xl bg-[#0B5D3B] py-3.5 text-[13.5px] font-semibold text-white shadow-sm transition hover:bg-[#084B30] disabled:opacity-50">{busy ? "Updating password…" : "Update password"}</button><button type="button" onClick={onBack} className="mx-auto block text-[12.5px] font-semibold text-slate-500 hover:text-slate-800">Back to sign in</button></form>}</EnterpriseAuthShell>;
}

export function VerificationView({ email, onBack, onResend, toMessage }) {
  const [cooldown, setCooldown] = useState(0); const [busy, setBusy] = useState(false); const [error, setError] = useState(null); const [sent, setSent] = useState(false);
  useEffect(() => { if (!cooldown) return; const timer = window.setInterval(() => setCooldown((value) => Math.max(0, value - 1)), 1000); return () => window.clearInterval(timer); }, [cooldown]);
  async function resend() { if (!email || cooldown) return; setBusy(true); setError(null); try { await onResend(email); setSent(true); setCooldown(45); } catch (resendError) { setError(toMessage(resendError)); } finally { setBusy(false); } }
  return <EnterpriseAuthShell title="Check your inbox" subtitle="Verify your email before creating or joining a Smart Manager workspace." asideTitle="Your workspace begins securely." asideCopy="Email verification protects your organisation and confirms the identity behind its first administrative account."><div className="space-y-4">{error && <AuthNotice>{error}</AuthNotice>}{sent && <AuthNotice type="success">A new verification email has been requested. Please allow a few minutes for delivery.</AuthNotice>}<div className="rounded-xl border border-slate-100 bg-slate-50 p-4"><Mail className="mb-3 text-emerald-700" size={20}/><p className="text-[13px] font-semibold text-slate-800">Open the verification email</p><p className="mt-1 text-[12px] leading-5 text-slate-500">We sent instructions to <span className="font-medium text-slate-700">{email || "your email address"}</span>. The link will securely continue your setup.</p></div><button type="button" disabled={busy || cooldown > 0 || !email} onClick={resend} className="w-full rounded-xl border border-slate-200 py-3 text-[13px] font-semibold text-slate-700 transition hover:bg-slate-50 disabled:cursor-not-allowed disabled:opacity-50">{busy ? "Resending…" : cooldown ? `Resend available in ${cooldown}s` : "Resend verification email"}</button><button type="button" onClick={onBack} className="mx-auto block text-[12.5px] font-semibold text-slate-500 hover:text-slate-800">Back to sign in</button></div></EnterpriseAuthShell>;
}
