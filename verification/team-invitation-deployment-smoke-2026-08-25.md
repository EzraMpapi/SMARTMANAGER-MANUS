# Team invitation deployment smoke check

**Date:** 25 August 2026

The deployed Render application responded successfully at:

- `https://smartmanager-manus-render.onrender.com/`
- `https://smartmanager-manus-render.onrender.com/app`

The public landing page and authenticated application entry shell rendered normally. The `/app` page presented the secure sign-in surface without a server-error page. A full Invite click test was not performed because it requires a real authenticated manager account; no credentials were entered or stored during this smoke check.
