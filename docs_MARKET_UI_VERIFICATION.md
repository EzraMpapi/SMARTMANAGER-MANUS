# Market data UI verification

The public dashboard shell was captured at 1280x720 and 390x844. Both viewports render without layout overflow in the public shell. The authenticated dashboard was not available in the sandbox screenshot session, so the BOT/DSE cards require an authenticated owner session for final visual confirmation. Server-side live validation independently confirmed 43 BOT exchange-rate rows and 21 DSE daily-summary rows from official public endpoints.
